FERMI_DEPLOYMENT=fermi-r6-mainnet
CLUSTER=mainnet-beta
CLUSTER_URL=https://api.mainnet-beta.solana.com
PROGRAM_ID=FRMiKrj2hQGvcZQtSDdiFRZ4cmaTjuc1QVkM2B5ShUvA
USER_KEYPAIR=/absolute/path/to/id.json
GROUP_PK=87qUKYQoK1f9gYQjYzw5NcRo7wx6VmfhTGJ7JenoeYAA
FERMI_ACCOUNT_PK=FERMI_ACCOUNT_PUBKEY
FERMI_ACCOUNT_NUM=0
FERMI_ACCOUNT_NAME=
EXECUTION_QUEUE_PK=EXECUTION_QUEUE_PUBKEY

# ── Fermi proxy gateway ───────────────────────────────────────────────────────
# All SDK calls now route through the proxy gateway. The API key is mandatory
# and is sent as the `x-api-key` header (HTTP) / metadata (gRPC). The proxy
# uses it for identity + per-key rate limiting.
FERMI_API_URL=https://v1.fermi.trade/prod
FERMI_API_GRPC_ADDR=v1.fermi.trade:443
FERMI_API_KEY=<enter api key>

RELAYER_MAX_FEE_LAMPORTS=AUTO
USDC_MINT=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
USDC_AMOUNT_UI=10
WITHDRAW_ALLOW_BORROW=false


PERP_MARKET_INDEX=0
PERP_ORDER_MARKET_INDEX=0
PERP_ORDER_SIDE=bid
PERP_ORDER_PRICE=100
PERP_ORDER_QUANTITY=0.01
PERP_ORDER_MAX_QUOTE_QUANTITY=
PERP_ORDER_CLIENT_ORDER_ID=
PERP_INTENT_CLIENT_ORDER_ID=
PERP_ORDER_TYPE=postOnlySlide
PERP_ORDER_SELF_TRADE_BEHAVIOR=decrementTake
PERP_ORDER_REDUCE_ONLY=false
PERP_ORDER_EXPIRY_TIMESTAMP=0
PERP_ORDER_MATCH_LIMIT=10
PERP_ORDER_MIN_EXECUTE_SLOT=
PERP_ORDER_EXPIRES_AT_SLOT=
PERP_ORDER_MAX_FEE_LAMPORTS=AUTO
DIRECT_ORDER_MARKET_INDEX=0
DIRECT_ORDER_SIDE=bid
DIRECT_ORDER_PRICE=100
DIRECT_ORDER_QUANTITY=0.01
DIRECT_ORDER_MAX_QUOTE_QUANTITY=
DIRECT_ORDER_CLIENT_ORDER_ID=
DIRECT_ORDER_TYPE=postOnlySlide
DIRECT_ORDER_SELF_TRADE_BEHAVIOR=decrementTake
DIRECT_ORDER_REDUCE_ONLY=false
DIRECT_ORDER_EXPIRY_TIMESTAMP=0
DIRECT_ORDER_MATCH_LIMIT=10
DIRECT_ORDER_EXPIRES_AT_SLOT=
DIRECT_ORDER_NONCE=
BOT_SIDE=bid
BOT_SPREAD_BPS=20
BOT_SIZE=0.01
BOT_INTERVAL_MS=2000
BOT_MAX_FEE_LAMPORTS=AUTO
COINGECKO_ASSET_ID=solana
COINGECKO_VS_CURRENCY=usd

# ── simulate / portfolio scripts ──────────────────────────────────────────────
# Used by `npm run simulate` and `npm run portfolio`. The harness's in-process
# /simulate cache has a default 15s TTL; the simulate script auto-warms via
# POST /simulate/warm before every run, but a real UI should warm once per
# trade-modal open and let subsequent keystrokes hit cache directly.
OWNER=
SIMULATE_OWNER=
SIMULATE_FERMI_ACCOUNT=
SIMULATE_MARKET=SOL-PERP
SIMULATE_SIDE=buy
SIMULATE_QUANTITY=1
SIMULATE_PRICE=
SIMULATE_ORDER_TYPE=limit
SIMULATE_REDUCE_ONLY=false
VIEW=optimistic

# ── smoke-check probes ────────────────────────────────────────────────────────
# Optional: setting USER_OWNER_PK lets smoke-check query a real
# /relayer/fees/status balance instead of just probing reachability.
USER_OWNER_PK=
